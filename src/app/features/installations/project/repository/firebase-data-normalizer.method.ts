/* @autogenerated */
import { QueryConstraint, where } from 'firebase/firestore';
import { Project } from '../model/project.model';
import { Observable, of } from 'rxjs';
export function firebaseDataNormalizer(data: Project): Observable<Project> {
  return of(data);
}
