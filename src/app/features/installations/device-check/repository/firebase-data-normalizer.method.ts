/* @autogenerated */
import { QueryConstraint, where } from 'firebase/firestore';
import { DeviceCheck } from '../model/device-check.model';
import { Observable, of } from 'rxjs';
export function firebaseDataNormalizer(data: DeviceCheck): Observable<DeviceCheck> {
  return of(data);
}
