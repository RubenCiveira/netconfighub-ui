/* @autogenerated */
import { DeviceCheck } from 'app/features/installations/device-check/model/device-check.model';
import { FormControl } from '@angular/forms';
import { input, effect, Component } from '@angular/core';

@Component({
  selector: 'device-check-view-detail',
  templateUrl: './device-check-view-detail.component.html',
  styleUrl: './device-check-view-detail.component.scss',
  standalone: false,
})
export class DeviceCheckViewDetailComponent {
  data = input.required<DeviceCheck | null>();
  form = new FormControl<DeviceCheck | undefined>({ value: undefined, disabled: true });

  public constructor() {
    effect(() => this.form.patchValue(this.data()));
  }
}
