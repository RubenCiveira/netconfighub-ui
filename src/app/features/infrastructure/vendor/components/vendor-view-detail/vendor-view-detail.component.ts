/* @autogenerated */
import { FormControl } from '@angular/forms';
import { Vendor } from 'app/features/infrastructure/vendor/model/vendor.model';
import { input, effect, Component } from '@angular/core';

@Component({
  selector: 'vendor-view-detail',
  templateUrl: './vendor-view-detail.component.html',
  styleUrl: './vendor-view-detail.component.scss',
  standalone: false,
})
export class VendorViewDetailComponent {
  data = input.required<Vendor | null>();
  form = new FormControl<Vendor | undefined>({ value: undefined, disabled: true });

  public constructor() {
    effect(() => this.form.patchValue(this.data()));
  }
}
