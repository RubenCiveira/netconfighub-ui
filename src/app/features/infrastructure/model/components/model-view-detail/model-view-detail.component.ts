/* @autogenerated */
import { Model } from 'app/features/infrastructure/model/model/model.model';
import { FormControl } from '@angular/forms';
import { input, effect, Component } from '@angular/core';

@Component({
  selector: 'model-view-detail',
  templateUrl: './model-view-detail.component.html',
  styleUrl: './model-view-detail.component.scss',
  standalone: false,
})
export class ModelViewDetailComponent {
  data = input.required<Model | null>();
  form = new FormControl<Model | undefined>({ value: undefined, disabled: true });

  public constructor() {
    effect(() => this.form.patchValue(this.data()));
  }
}
