/* @autogenerated */
import { Model } from '../model/model.model';
import { Observable, of } from 'rxjs';
export function appwriteReader(data: any): Model {
  const copy = { ...data } as any;
  copy.vendor = { $ref: data.vendor, $id: (data as any).vendor?.$id };
  copy.type = { $ref: data.type, $id: (data as any).type?.$id };
  return copy;
}
