/* @autogenerated */
import { ConfigTemplate } from 'app/features/infrastructure/config-template/model/config-template.model';
import { FormControl } from '@angular/forms';
import { input, effect, Component } from '@angular/core';

@Component({
  selector: 'config-template-view-detail',
  templateUrl: './config-template-view-detail.component.html',
  styleUrl: './config-template-view-detail.component.scss',
  standalone: false,
})
export class ConfigTemplateViewDetailComponent {
  data = input.required<ConfigTemplate | null>();
  form = new FormControl<ConfigTemplate | undefined>({ value: undefined, disabled: true });

  public constructor() {
    effect(() => this.form.patchValue(this.data()));
  }
}
