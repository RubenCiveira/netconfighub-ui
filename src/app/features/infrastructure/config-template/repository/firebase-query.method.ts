/* @autogenerated */
import { QueryConstraint, where } from 'firebase/firestore';
import { ConfigTemplateFilter } from '../model/config-template.model';
import { Observable, of } from 'rxjs';
export function firebaseQuery(query: ConfigTemplateFilter): Observable<QueryConstraint[]> {
  const constraints: QueryConstraint[] = [];
  return of(constraints);
}
