/* @autogenerated */
import { Client } from 'app/features/organization/client/model/client.model';
import { FormControl } from '@angular/forms';
import { input, effect, Component } from '@angular/core';

@Component({
  selector: 'client-view-detail',
  templateUrl: './client-view-detail.component.html',
  styleUrl: './client-view-detail.component.scss',
  standalone: false,
})
export class ClientViewDetailComponent {
  data = input.required<Client | null>();
  form = new FormControl<Client | undefined>({ value: undefined, disabled: true });

  public constructor() {
    effect(() => this.form.patchValue(this.data()));
  }
}
