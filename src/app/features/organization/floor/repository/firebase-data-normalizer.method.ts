/* @autogenerated */
import { QueryConstraint, where } from 'firebase/firestore';
import { Floor } from '../model/floor.model';
import { Observable, of } from 'rxjs';
export function firebaseDataNormalizer(data: Floor): Observable<Floor> {
  return of(data);
}
