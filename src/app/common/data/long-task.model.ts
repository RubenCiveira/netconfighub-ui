/* @autogenerated */
export interface LongTask {
  uid: string;
  detailStatus: 'PENDING' | 'PROCESSING' | 'FINISHED' | 'FAILED';
  startTime?: Date;
  endTime?: Date;
  steps: LongTaskStep[];
}

export interface LongTaskStep {
  name: string;
  stepStatus: 'PENDING' | 'PROCESSING' | 'FINISHED' | 'FAILED';
  startTime?: Date;
  endTime?: Date;
  totalItems: number;
  processedItems: number;
  oks: string[];
  warns: BatchResultErrorItem[];
  errors: BatchResultErrorItem[];
}

export interface BatchResultErrorItem {
  item: string;
  fails: MultipleErrorCodes[];
}

export interface MultipleErrorCodes {
  code: string;
  violation: string;
  wrongValues: { field: string; error: string; wrong: string }[];
}
