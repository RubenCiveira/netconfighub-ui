/* @autogenerated */
import { CommonModule } from '@angular/common';
import { Component, input } from '@angular/core';
import { ReactiveFormsModule } from '@angular/forms';
import { TranslocoModule } from '@jsverse/transloco';
import { BaseEditable } from '../base-editable';
import { MatFormFieldModule } from '@angular/material/form-field';
import { MatInputModule } from '@angular/material/input';

@Component({
  template: `
    <mat-form-field>
      <mat-label>{{ label() + '.label' | transloco }}</mat-label>
      <span matPrefix>&nbsp;{{ currency() }}&nbsp;</span>
      <input
        matInput
        [required]="required()"
        type="number"
        placeholder="{{ label() + '.placeholder' | transloco }}"
        [attr.step]="step()"
        [attr.min]="minValue()"
        [attr.max]="maxValue()"
        [formControl]="control"
      />
      @for (error of errorMessages(); track $index) {
        <mat-error>
          {{ label() + '.error.' + error | transloco }}
        </mat-error>
      }
    </mat-form-field>
  `,
  selector: 'editable-money',
  imports: [ReactiveFormsModule, MatFormFieldModule, MatInputModule, CommonModule, TranslocoModule],
  providers: BaseEditable.getAccessorProvider(EditableMoneyComponent),
  standalone: true,
})
export class EditableMoneyComponent extends BaseEditable<string> {
  minValue = input<number>();
  maxValue = input<number>();
  step = input<number>(0.01);
  currency = input<string>('€');
}
