/* @autogenerated */
import { Injectable } from '@angular/core';

@Injectable()
export class LocalRouterService {
  go(route?: string) {
    const ctx = (this as any).context;
    if (ctx && ctx.param && ctx.bind) {
      const target = {} as any;
      if (route) {
        target[ctx.param] = route;
      }
      ctx.bind.go(target);
    }
  }
}
